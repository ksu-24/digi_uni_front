"use client"

import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#000000",
        },
        secondary: {
            main: "#FEFFE6"
        },
        info: {
            main: "#D5DAFF"
        }
    }
})

export default theme;